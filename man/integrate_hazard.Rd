% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{integrate_hazard}
\alias{integrate_hazard}
\alias{cum_haz}
\title{Create a cumulative hazard function by integrating a hazard rate}
\usage{
integrate_hazard(haz)
}
\arguments{
\item{haz}{hazard rate function \eqn{h(t, ...)}}
}
\value{
A function that computes the cumulative hazard at time t
}
\description{
Given a hazard rate function \eqn{h(t)}, returns a function computing
\eqn{H(t) = \int_0^t h(u) du} via numerical integration.
}
\examples{
# Exponential hazard h(t) = lambda
haz <- function(t, ...) rep(0.5, length(t))
H <- integrate_hazard(haz)
H(2)  # Should be 1.0 (0.5 * 2)
}
